/* eslint-disable react/prop-types */
// NavBar.js


// eslint-disable-next-line no-unused-vars
const NavBar = ({ onNavClick }) => {
  return (
    <nav>
      <ul>
        {/* <li onClick={() => onNavClick('search')}>Search</li> */}
      </ul>
    </nav>
  );
};

export default NavBar;
